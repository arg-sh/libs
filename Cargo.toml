[workspace]
members = ["shared", "jaml-builtin"]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
libc = "0.2"

[profile.release]
lto = true
opt-level = "s"
strip = true
panic = "unwind"
codegen-units = 1
